#include <stdio.h>
/**
 * main - prints "with proper grammer, but the outcome is
 * a pice of art," followed by newline
 * Return: Always 0.
 */

int main(void)
{
	printsf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
