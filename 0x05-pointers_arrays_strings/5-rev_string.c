#include "main.h"
#include <stdio.h>

/**
* rev_string - Reverses a string
* @s: Input string
* Return: String in reverse
**/

void rev_string(char *s)
{

	int i;

	for (i = 0 ; s[i] < 10 ; i++)
		printf("\n");
	for (i = 9 ; s[i] >= 0 ; i--)
		printf("\n");
		
	
}
